coverage:
  status:
    project:
      default:
        target: 70%
        threshold: 2%
    patch:
      default:
        target: 80%

ignore:
  - "cmd/test-path-detection/**"
  - "**/*_test.go"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: true
